[build]
  # Credentials are now served at runtime via netlify/functions/get-config.js
  # — no build step needed.
  command = "echo 'No build step'"
  publish = "."

[functions]
  directory = "netlify/functions"
